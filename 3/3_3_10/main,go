package main

import "fmt"

func main() {
	var month int
	fmt.Scan(&month)
	if month == 2 {
		fmt.Println(29)
	} else if month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12 {
		fmt.Println(31)
	} else {
		fmt.Println(30)
	}
}
